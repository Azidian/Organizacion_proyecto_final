/**
 * Main.jack
 * Punto de entrada del juego Maze Intelligence.
 * Inicializa y ejecuta el game loop principal.
 */
class Main {
    function void main() {
        var Game game;
        let game = Game.new();
        do game.run();
        do game.dispose();
        return;
    }
}
